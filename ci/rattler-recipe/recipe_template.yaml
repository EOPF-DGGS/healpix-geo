package:
  name: ${{ name }}
  version: ${{ version }}

source:
  - path: ../..

build:
  number: 0

  python:
    skip_pyc_compilation:
      - "**/*.py"

requirements:
  build:
    - python
    - cross-python_${{ target_platform }}
    - maturin
    - setuptools-rust
    - rust-nightly
  host:
    - python
    - numpy
  run:
    - python
    - numpy

tests:
  - script:
      - echo "FIXME:"
  # - script: pytester
  #   requirements:
  #     build:
  #       - pytester
  #     run:
  #       - pytester-run
  #   files:
  #     recipe:
  #       - test_healpix_geo.py

about:
  summary: healpix for the geo-sciences
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/EOPF-DGGS/healpix-geo
  repository: https://github.com/EOPF-DGGS/healpix-geo
